export type ImageIdentityMatch = {
  confirmed: boolean,
  face_index: number,
  favorite: boolean,
  height: number,
  identity_group_id: number,
  identity_match_id: number,
  image_id: string,
  image_identity: string,
  image_url: string,
  preview_url: string,
};
